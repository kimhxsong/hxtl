# 트리

## 트리(tree)
트리는 비선형 자료구조로 계층 관계(부모-자식)를 나타내는 데 사용한다. 각 노드는 다른 노드와 방향이 있는 간선(directed edge)으로 연결된다.

## 루트(root)
들어오는 간선이 없는 유일한 노드로 트리의 최상위 노드

## 노드(node)
트리의 기본 요소로, 각 노드는 널 값이나 자식을 가리키는 두 개의 포인터와 데이터를 가진다.

## 간선(edge)
트리의 기본 요소로, 두 노드를 연결하는데 사용한다.

## 경로(path)
간선으로 연결된 노드의 순서 목록

## 말단(leaf)
자식이 없는 노드

## 높이/깊이(height/depth)
루트부터 말단까지 가장 긴 경로의 간선의 수

## 자식(children)
한 노드의 바로 아래로 뻗어 나가는 노드

## 부모(parent)
자식 노드의 바로 위에 연결된 노드

## 형제(sibling)
부모가 같은 자식 노드

## 조상(ancestor)
자식에서 부모로 이동을 계속해 닿을 수 있는 노드

## 레벨(level)
루트에서부터 어떤 노드까지의 경로에 있는 간선의 수

## 이진 트리(binary tree)
각 노드에 왼쪽 자식과 오른쪽 자식이라는 최대 2개의 자식 노드를 가질 수 있는 트리

## 완전 이진 트리(complete binary tree)
마지막 레벨을 제외한 모든 레벨이 완전히 채워진 이진 트리
왼쪽의 모든 노드가 먼저 채워진 다음에 오른쪽이 채워진다.

## 정 이진 트리(full binary tree)
각 노드가 모두 0개 또는 2개의 자식이 있는 이진 트리

## 포화 이진 트리(perfect binary tree)
말단 노드를 제외한 모든 노드가 정확히 두 개의 자식 노드를 갖는 정 이진 트리의 한 종류

## 우편향 이진 트리(right skewed binary tree)
말단 노드를 제외한 모든 노드가 오른쪽 자식만을 가지는 이진 트리

## 좌편향 이진 트리(left skewed binary tree)
말단 노드를 제외한 모든 노드가 왼쪽 자식만을 가지는 이진 트리

## 높이 균형 이진 트리(height-balanced binary tree)
각 노드의 왼쪽과 오른쪽 하위 트리의 높이가 최대 1만큼 차이 나는 이진 트리
높이 균형 이진 트리의 예로 AVL트리, 레드-블랙 트리가 있으며, 완전 이진 트리도 높이 균형 이진 트리이다.


## 전위 순회(pre-order traversal)

## 후위 순회(post-order traveral)

## 중위 순회(in-order traversal)

## 레벨 순회(level order traversal)




